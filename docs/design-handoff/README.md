# Design System — Handoff / Export (Claude Design)

Export do **novo Design System Simplafy** (Claude Design) — referência de tokens, tipografia
(Causten/Geist), componentes e ui_kits por vertical (hub/saude/seguros/reach/site).

> Versionado aqui porque a referência vivia em `.tmp/` (temporário) e foi perdida. É **referência
> de design** — a fonte canônica BUILDÁVEL do DS continua sendo `src/` (publicada como `@simplafy-tec/ui`).
> Este `docs/design-handoff/` NÃO é typechecked (tsconfig = `src/**`) nem publicado (package `files` = dist + globals.css).

## O que NÃO está aqui (propositalmente)
- Fontes (`.ttf`/`.woff`) — já em `src/`/pacote. Imagens/uploads/logos pesados — arquivados fora do git (no zip original `[DS] Design System (1).zip`).

## Conteúdo
COMPONENTS.md · MIGRATION.md · AUDIT.md · globals.css · colors_and_type.css · components/ · apps/ · ui_kits/{hub,saude,seguros,reach,site}.
